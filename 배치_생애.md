# 배치(마이크로배치)는 어떻게 만들어지고 굴러가는가

> 수정 후 목표 동작을 풀어 쓴 문서. 헷갈리기 쉬운 지점을 먼저 짚는다.

## 가장 헷갈리는 것부터 — "배치는 한 번에 처리되는 덩어리가 아니다"

배치(mb)를 만들면 GPU가 그걸 한 번에 쭉 처리하고 끝나는 게 아니다.

배치 하나는 **80개 레이어를 한 사이클씩 돌면서**, 매 사이클마다
`QKV → (prefill-attn ‖ decode-attn) → O-projection` 4개 연산을 한다.
80사이클을 다 돌면 = decode 토큰 1개 생성. 그리고 또 80사이클을 돈다.

즉 배치는 "한 번 처리하고 사라지는 덩어리"가 아니라, **계속 도는 살아있는 단위**다.
prefill이 길면(예: 6만 토큰) 한 사이클에 다 못 하고, 사이클마다 조금씩(chunk) 잘라
먹으며 여러 사이클에 걸쳐 처리한다.

---

## 세 가지 한계 — 목적이 다르다 (헷갈리면 안 됨)

원래는 KV 캐파 하나로 배치 크기까지 겸했던 게 문제였다. 목적이 다른 세 개로 나눈다.

| 한계 | 성격 | 무엇을 막나 | 어떻게 정하나 |
|---|---|---|---|
| **KV 캐파** | 하드 | 메모리 초과(OOM). 전체 동시 보유 KV 한계 | 메모리 (4M 고정) |
| **배치 크기 캐파** (좌석) | 하드 | head-of-line blocking. 한 배치의 **요청 개수** | 스윕 {256, 512} |
| **윈도우 캐파** (버스 대수) | 하드 | — (staggering 깊이). 동시 생존 배치 수 | **작게 고정 (3)** |

- **KV 캐파** = 무게 한계. 메모리니까 절대 못 넘는다. 여러 배치가 나눠 쓴다.
- **배치 크기 캐파** = 좌석 수. 요청 *개수* 상한 (KV 길이가 아니라 사람 수).
- **윈도우 캐파** = 동시에 굴리는 버스 대수. staggering(한 배치가 PIM 쓸 때 다른
  배치가 GPU 씀)은 2~3대면 충분. **메모리 한계까지 늘리면 안 된다** — 늘리면 동시
  KV가 다시 캐파 전체를 먹어 단일 배치 독점 병으로 되돌아간다.

세 개는 서로 다른 걸 막으므로 분리해야 한다. 그리고 이 위에서 밸런스(소프트)가
prefill chunk를 동적으로 조절한다 — 상한은 천장, 밸런스는 천장 아래 조절이라 충돌 없음.

---

## 배치 하나의 생애 — 버스 비유

배치(mb)를 **버스**라고 하자.

### 1. 탑승 — 누구를 태울까 (하드 한계만 본다)

정류장(대기 큐)에서 도착 순서대로 승객(요청)을 태운다. 이때 보는 건 딱 두 개:

- **좌석 수** = 배치 크기 상한 (예: 256개 요청)
- **무게 한계** = KV 캐파 (4M 토큰)

둘 중 **먼저 차는 쪽**에서 탑승 마감.

- 컨텍스트 긴 요청(long): 무게(KV)가 먼저 참 → 40명쯤에서 마감
- 컨텍스트 짧은 요청(short): 좌석(256)이 먼저 참 → 256명에서 마감

→ **탑승은 밸런스를 안 본다. 오직 하드 한계(좌석·무게)로만 누가 타는지 정한다.**

### 2. 짐 내리는 양 정하기 — 밸런스 (탑승 직후, 같은 admission tick)

태운 승객이 정해졌다. 이제 **prefill 승객의 짐(프롬프트)을 한 사이클에 얼마나
내릴지**(chunk 크기)를 정한다. 이게 밸런스다.

- decode 승객들이 이번 사이클에 PIM을 얼마나 쓸지(t_pim) 계산 → GPU가 놀지 않게
  **prefill 짐을 그만큼 더 잘게/많이 내리도록** chunk 예산을 키운다.
- 핵심: 밸런스가 정하는 건 **"prefill을 한 사이클에 얼마씩 자를지(예산)"**다.
  decode를 새로 태우는 게 아니다 (decode는 1단계에서 이미 탑승 끝).

→ **밸런스 = 탑승 다음이 아니라, 탑승 직후 같은 순간에 prefill chunk 예산을 정함.**

**밸런스 입력은 "미래 decode 길이"가 아니라 "현재 KV 길이"다.** 이게 핵심이다.

- decode는 매 사이클 토큰 1개씩(autoregressive)이라, 그 토큰의 attention 시간은
  **지금 그 요청의 KV 길이**(프롬프트 + 지금까지 생성분)로 정확히 계산된다.
- 즉 "앞으로 decode가 몇 개 더 나올지" 예측이 **전혀 필요 없다**. LLM이라 종료
  시점을 몰라도 상관없다.
- 밸런스는 매 사이클(정확히는 admission tick마다) "지금 살아있는 decode 요청들 ×
  각자 현재 KV → PIM 시간 확정 → prefill chunk 조절"하는 **상태 기반 피드백**이다.
  요청이 끝나서 빠지면 다음번에 다시 계산한다(자기 보정).
- 트레이스의 decode 길이(예: 350)는 **종료 시점 판정용**일 뿐 밸런스 입력이 아니다.
  실제 서버의 가변 길이/EOS여도 밸런스 로직은 그대로 돈다.

### 3. 운행 — 자르는 건 두 동작 (성격이 다름)

prefill 자르기는 **성격이 다른 두 동작**으로 나뉜다.

| 동작 | 언제 | 무엇을 | 횟수 |
|---|---|---|---|
| **예산 정하기** (밸런스) | admission tick 주기 | "한 사이클에 prefill 몇 토큰" 숫자만 | 드물게 (tick마다) |
| **실제 잘라 떼기** | 매 사이클 | 그 예산대로 prefill을 실제로 잘라 처리 | 자주 (소진까지 반복) |

예: prefill 6만 토큰, 예산이 512로 정해지면 → 매 사이클 512씩 약 117번 잘라 먹는다.
예산은 가끔 (재)조정, 실제 절단은 매 사이클.

→ **"두 번 자른다"보다 "한 번 예산 정하고, 매 사이클 그 예산대로 떼어낸다"가 정확.**

### 4. 합류 — 자리가 나면 대기 승객을 태운다 (운행 중)

버스가 돌다가 승객 하나가 목적지(max_tokens)에 도착해 내리면, **좌석과 무게가
풀린다**. 그 빈 만큼 정류장(큐)에서 대기하던 신규 승객을 태운다.

- 합류 가능량 = min(좌석 여유, 무게 여유) — 둘 중 빡빡한 쪽까지만.
- 태우는 대상 = **큐의 신규 요청** (다른 버스에서 빼오는 게 아님).

**prefill 도 decode 도 둘 다 합류한다 — 단 방식이 다르다.**

- **prefill 합류** — 짐(프롬프트)이 커서 한 사이클에 다 못 내리므로, 자른 짐
  (chunk)으로 태운다. 태운 뒤 2단계(밸런스)가 사이클당 자를 양을 정한다.
- **decode 합류** — decode는 한 사이클에 토큰 1개씩(autoregressive)이라 **자를 수
  없다.** 그래서 **통째로** 태운다. 자리(좌석+무게)만 나면 큐의 decode 요청을 그대로
  배치에 넣어 PIM 빈자리를 채운다.

→ "chunked"는 prefill에만 해당. decode는 쪼개는 게 아니라 통째로 **모으는(batching)**
것. 둘 다 합류 대상이라는 점은 같다.

**decode 합류는 공짜가 아니다 — GPU에도 일이 늘어난다.** decode 요청도 매 사이클
**QKV + O-projection을 GPU에서** 한다(PIM이 하는 건 decode-attn 하나뿐). 그래서
decode를 N개 더 태우면 **GPU projection도 N에 비례해 늘어난다**. 또 그 요청들의 KV
캐시가 메모리에 상주해야 하므로 **KV 캐파 여유도 그만큼 있어야** 채울 수 있다. 즉
"PIM 비니까 decode 때려넣으면 끝"이 아니라, GPU도 같이 커지므로 **컨텍스트가 충분히
길 때(>56K)** 만 PIM 증가분이 GPU 증가분을 넘어선다.

**"긴 decode가 합류하면 PIM이 병목 되나?" — 기본적으로는 안 된다 (밸런스가 자기
보정).** decode가 합류하면 그 KV가 다음 밸런스 계산에 자동 반영되어 t_pim이 커지고,
밸런스가 **기존 prefill을 더 잘게/많이 잘라(chunk ↑) GPU를 키워** PIM을 따라잡게
한다. 합류는 길이로 고르는 게 아니라 도착 순서(FIFO)라 "긴 걸 골라 터뜨리는" 일도
구조적으로 없다.

- **유일한 예외 = 배치에 prefill 일감이 없을 때.** prefill이 다 소진된 순수 decode
  구간에서는 키울 chunk가 없어 밸런스가 무력해지고, PIM이 그냥 병목이 된다.
- 이건 비정상이 아니다 — long-context decode가 충분히 많으면 PIM이 bound 되는 게
  맞다(원하던 동작). 이때 GPU 빈자리는 **prefill 요청을 합류**시켜 채운다.
- 그래서 합류가 양방향이어야 한다: PIM 비면 decode 합류로 채우고, prefill 일감이
  마르면 prefill 합류로 GPU를 채운다 — 서로의 빈자리를 메운다.

→ 이게 연속 배칭. "누구를 태울까(합류)"와 "짐을 얼마씩 내릴까(밸런스)"는 별개라
충돌하지 않는다.

### 5. 종료 — 언제 버스를 세우나

두 조건이 **동시에** 성립할 때 버스 운행 종료(evict):

1. 안의 승객이 **모두 도착**(decode 전부 max_tokens 완료)
2. 정류장에 **더 태울 대기 승객 없음**(큐 빔)

큐에 승객이 있는 한 합류로 버스는 계속 굴러간다. 무한히 안 끝나는 건 도착률이
처리율을 영구 초과하는 과부하(DDoS급)일 때뿐이고, 그건 admission control이 막을
영역이지 정상 동작이 아니다.

**종료에 이르는 인과 — 밸런스가 합류 게이트를 닫아야 소진이 진행된다.**

decode를 계속 합류시키면 안의 decode가 줄어들 새가 없어 배치가 안 끝난다. 그러니
배치가 끝나려면 먼저 **합류가 멈춰야** 하는데, 합류를 멈추는 원리가 곧 밸런스다.

1. PIM(decode 시간)과 GPU(prefill 시간) 레이턴시가 **맞으면(in_band)** → 더 채울
   이유가 없음 → **합류 게이트 닫힘** (decode를 더 안 태움).
2. 게이트가 닫히면 안의 decode들이 토큰을 다 뽑고 하나씩 빠짐 → **소진.**
3. 소진 + 큐도 비면 → 종료.

즉 밸런스는 종료의 *직접* 조건이 아니라 **"합류를 멈추게 하는 선행 원인"**이고,
그 결과로 소진이 일어나 종료된다. (밸런스 맞음 → 합류 중단 → decode 소진 → 종료)

단, "합류를 안 함"은 두 경우에서 나온다: ① 큐에 일감은 있지만 밸런스가 맞아 안
넣음(정상 평형 — 균형 유지하며 천천히 소진), ② 큐가 비어 넣을 게 없음. 정상 부하
(큐 안 마름)에선 ①이 지배적이다.

---

## 한 줄 요약

**태우고(하드 한계) → prefill 짐 내릴 양을 밸런스가 정하고(같은 tick) → 80층을
사이클로 돌며 예산대로 잘라 먹고 → 자리 나면 대기 요청 합류 → 다 내리고 큐도 비면
종료.**

- 탑승은 좌석·무게(배치 크기·KV 캐파)로만.
- 밸런스는 prefill을 사이클당 얼마씩 자를지(예산)로 GPU/PIM 시간을 맞춤.
- 배치는 한 번에 처리되는 덩어리가 아니라 사이클로 도는 살아있는 단위.
