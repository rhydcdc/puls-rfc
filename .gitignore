# Local-only working area (not for public exposure)
_local/
# ideas/ — 이제 추적(pre-positioning 프로토타입). build 바이너리는 아래 `build/` 로 계속 제외.
# ideas/experiments/ — 로컬 실험 작업장(엔진 사본 + REPORT_X) — 비공개, 푸시 금지.
ideas/experiments/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Temp
*.tmp
*.bak

# Figures: keep only the PNG embedded in README; ignore source/alternative formats
figures/*
!figures/instance_disaggregation.png

# Local LLM behavior guide (not for public exposure)
CLAUDE.md

# Python implementation/ — superseded by the generalized C++ scheduler (puls-engine/).
# Kept LOCAL as the detailed event-sim / evaluator reference; NOT published to GitHub.
# (puls-engine is the generalized, doc-consistent, performant replacement for the scheduler.)
implementation/

# Implementation plan documents (local working notes, code-only commits)
implementation/plans/

# Local report drafts (markdown synthesized into README; not separately published)
implementation/reports/

# Top-level PLAN/MODULES docs (Korean working notes; English versions to be published later)
implementation/PLAN.md
implementation/MODULES.md

# Claude Code per-user permission overrides (env-specific paths)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/
.venv/
venv/

# --- Phase-2 measurement scratch & regenerable artifacts (reproduction-only) ---
# floor_proof.py (idle measurer) — removed from docs; idle≈0 is by-construction
# (삼중균형 정의, §6.8 cpp 구성검증으로 충분). source 더 이상 추적 안 함.
implementation/analysis/floor_proof.py
# floor_proof.py / measure_steady.py console-dump outputs (regenerate by re-running)
implementation/analysis/floor_*.txt
implementation/debug_phase2/steady_*.txt
implementation/analysis/_scratch_*/
implementation/debug_phase2/_scratch_*/

# Data folder — traces · measurements (local-only, not published). sweeps regenerable via
# debug_phase2/gen_sweep_traces.py; longbench traces + aux1 T4 JSON kept local only.
implementation/data/

# Local-only conceptual notes (personal understanding notes, not published)
*.local.md

# Debug/dev work folders (Korean working notes · protos · reports — local-only, not published)
implementation/debug_phase1/
implementation/debug_phase2/
